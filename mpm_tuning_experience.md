# 🚀 可微 MPM 仿真稳定性与调参实战总结

在对“双提拉布料 (Double Lift Cloth)”场景进行系统辨识（System ID）的过程中，我们成功解决了从 Frame 10 到 Frame 84 期间频繁出现的粒子爆炸问题。以下是核心经验总结。

---

## 1. 核心矛盾分析：为什么布料模拟容易“炸”？
*   **质量奇异性**：布料粒子质量极轻（$10^{-4}$）。在力模式下，加速度 $a = F/m$ 极易溢出。
*   **几何拉断 (Geometric Tearing)**：局部拉伸率超过网格感应范围（核函数半径），粒子失去连接力，瞬间释放的弹性能诱发爆炸。
*   **CFL 条件击穿**：粒子移动速度或波速超过了 $\Delta x / dt$ 的理论极限。

---

## 2. 稳定性加固的三大“杀手锏”

### A. 模式切换：从“力”到“质量无关加速度” (Mass-Independent PD)
*   **物理公式**：直接控制粒子加速度，而非通过除以质量计算力。
    $$\mathbf{a} = K_p \cdot \underbrace{[(\mathbf{x}_{target} - \mathbf{x}_{curr}) - \mathbf{offset}_0]}_{\text{跟随位移误差}} + K_d \cdot \underbrace{(\mathbf{v}_{target} - \mathbf{v}_{curr})}_{\text{速度同步误差}}$$
*   **执行逻辑**：
    1. 计算目标加速度 $\mathbf{a}$。
    2. 计算子步速度增量：$dv = \mathbf{a} \cdot dt$。
    3. 全局子步限速：$\text{clamp}(\sum dv, -1.0, 1.0)$。
*   **核心收益**：彻底解耦了控制器与粒子质量。无论布料多轻，它都能以可控的、恒定的加速度跟随控制器，消除了“除以极小质量”导致的数值奇异。

### B. 几何守卫：SVD 奇异值状态保护
*   **方案**：在计算应力前，对变形梯度 $F$ 进行 SVD 分解，将其奇异值（主伸长率）限制在 `[0.3, 5.0]`。
*   **收益**：在物理计算前强行“修正”极度畸变的几何形状，确保应力永远是物理合法的，防止产生导致梯度爆炸的 `Inf` 应力。

### C. 累加保护：子步总速度限速器 (Total dv Clamping)
*   **方案**：先累加所有控制器点对该粒子的加速度贡献，然后对**总速度变化量**执行严格截断。
*   **收益**：防止了多个控制点重叠抓取时产生的“力量热点”效应。

---

## 3. 分辨率降维的“降维打击” —— 成功的关键
将 `grid_res` 从 64 降低到 **32** 是本次成功的核心决策，原因如下：
1.  **CFL 余量翻倍**：网格间距 $\Delta x$ 增加了一倍，使同样的 $dt$ 能够承载更高的速度和波速，极大放宽了稳定性上限。
2.  **强制防止撕裂**：粒子的“感应手”变长了（$2\Delta x$ 变大）。原本在 64 分辨率下会拉断的位移，在 32 分辨率下依然处于同一个网格感应区内，从根本上杜绝了物理断裂。
3.  **数值平滑**：粗网格天然具有滤波器作用，能抹平控制点附近的局部受力峰值，使提拉过程更加均匀稳定。

---

## 4. 关键配置项清单 (dt=1e-4)

| 参数项 | 推荐值 | 物理意义 |
| :--- | :--- | :--- |
| `grid_res` | `32` | **最重要的稳定性参数**，提供巨大的 CFL 余量。 |
| `damping` | `2.0` | 适度的全局能量耗散。 |
| `controller_stiffness` | `5000` | 加速度模式下的位置跟随增益。 |
| `controller_damping` | `500` | 高阻尼确保提拉过程无振荡。 |
| `total_dv_clamp` | `1.0` | 允许粒子在极端情况下快速跟上控制器，防止滞后拉断。 |

---
*结论：在 Differentiable MPM 任务中，稳健的运动学控制（加速度模式）配合适度的空间精度牺牲（低分辨率网格），是保证长序列模拟不爆炸的黄金准则。* 🚀
